# 2026-06-09-004 — MCP Server Built and Registered

## Date
2026-06-09

## What was done
- Installed @modelcontextprotocol/sdk
- Built mcp-server.mjs — stdio-based MCP server exposing two tools:
  - `search_tools(query?, category?, tag?)` — full-text search across tools.json
  - `list_tools()` — one-line summary of all tools
- Registered in ~/.claude/.mcp.json alongside playwright and vidiq
- Enabled in ~/.claude/settings.json under enabledMcpjsonServers

## How it works
Claude Code starts the server automatically at session start (node mcp-server.mjs via stdio).
Every Claude session on the MBP can now call search_tools or list_tools directly as tool calls.
No per-project CLAUDE.md instruction needed — it's global.

## Also done this session
- Fixed global CLAUDE.md: removed broken reference to deleted AI-TOOLS-REFERENCE.md, replaced with query.mjs command

## What comes next
- Task 3: add ingest-screenshot.mjs for screenshot → tools.json via Claude vision
- Restart Claude Code session to pick up the new MCP server
