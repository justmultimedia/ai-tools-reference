Source: https://youtube.com/shorts/BhIVDXpBHMs?is=MmpXM2HXVothIfDi
Title: NVIDIA open-sources 10x faster computer vision model with #NVIDIA #opensource #computervision
Channel: AI Honeycove

Kind: captions Language: en Nvidia just open-sourced a computer Nvidia just open-sourced a computer Nvidia just open-sourced a computer vision model that's 10 times faster than vision model that's 10 times faster than vision model that's 10 times faster than top models, and it's kind of insane. top models, and it's kind of insane. top models, and it's kind of insane. Most vision models today predict Most vision models today predict Most vision models today predict bounding boxes step-by-step, bounding boxes step-by-step, bounding boxes step-by-step, corner-by-corner, token-by-token, but corner-by-corner, token-by-token, but corner-by-corner, token-by-token, but this one changes that. It uses something this one changes that. It uses something this one changes that. It uses something called parallel box decoding. Instead of called parallel box decoding. Instead of called parallel box decoding. Instead of predicting pieces, it predicts the predicting pieces, it predicts the predicting pieces, it predicts the entire box at once. That's why it's up entire box at once. That's why it's up entire box at once. That's why it's up to 10 times faster than models like to 10 times faster than models like to 10 times faster than models like Gwen-3-VL, and they trained it on Gwen-3-VL, and they trained it on Gwen-3-VL, and they trained it on massive data, over 100 million queries massive data, over 100 million queries massive data, over 100 million queries and hundreds of millions of boxes. It's and hundreds of millions of boxes. It's and hundreds of millions of boxes. It's fully open-sourced on Hugging Face and fully open-sourced on Hugging Face and fully open-sourced on Hugging Face and GitHub.
