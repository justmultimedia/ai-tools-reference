Source: https://www.instagram.com/reel/DaNb20tD5My/?igsh=dmxnaXNta3Vqa21h
Title: Video by kodekloud
Channel: kodekloud
Transcript: transcribed locally with Whisper

What is loop engineering? When you work with an LLM like Clod or ChatGPT, you give it a prompt, and the LLM gives back an output. You read this output, and if it's not quite what you wanted, you tweak the prompt and try again. This is basically prompt engineering, and you do it over and over until you're finally happy with the result. If you look carefully, what you're really doing is running a loop. You prompt, you get an output, improve the prompt, and try again. So what if you put an AI agent in your seat to run this loop for you? That's what people are starting to call loop engineering. Instead of you choosing the next step every single time, you build a loop that does it for you. The loop hands the task to the model, lets the model use different tools, checks what actually happened, and compares it against the goal. If the goal isn't reached, the loop runs again. To make this concrete, let's take an example. An agent whose job is to fix a bug in your codebase. The task is simple. Make the failing test cases pass. The most important piece of any loop is knowing when it stops. Because if you never stop the loop, your claude or chatGPT bill goes completely out of control. In our example, we stop the loop the moment all the test cases pass. Or we stop it after 15 runs no matter what so a hallucinating or stuck agent can't burn money all night. But now you've got a new problem. After a dozen loops, the agent has accumulated a ton of files and test logs in its context window. And we know that context is limited. If the agent loses track of the original goal, it starts to drift, and it can change code that was never broken. To fix this, we need to do context management. You keep the latest error and the file in question, summarize the old steps into a short note, and remind the agent of the goal on every cycle. Sometimes the agent runs a command and it just fails. Maybe the test command is wrong. Maybe a package is missing. Maybe the error is nothing like what the agent expected. Without a plan, the agent might just stop right there. But inside a loop, this error becomes useful. You feed the message back to the model and let it decide the next step. And the good news is, you don't have to build these loops by hand. Frameworks like Laingraph let you define the loop as a set of steps. And an agent SDK like the Cloud Agent SDK runs the whole cycle for you. So loop engineering isn't about finding one magic program. It's about finding the right way to build a loop. So loop engineering isn't about finding one magic program. It's about finding the right way to prompt. It's about building a good loop around the LLM. And a good loop really comes down to three things. A clear stop condition, so it always ends. Solid context management, so the agent doesn't drift. And error recovery, so mistakes become feedback instead of dead ends.
