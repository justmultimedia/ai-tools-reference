Source: https://youtube.com/shorts/OjhUwhilobI?is=lTESqXult9F7Ahb0
Title: How to Get $200 of Claude Code for $7
Channel: Eric Tech

Kind: captions Language: en You can get $200 of Claude Code for You can get $200 of Claude Code for You can get $200 of Claude Code for about $7. DeepSeek [music] just dropped about $7. DeepSeek [music] just dropped about $7. DeepSeek [music] just dropped V4 Pro and it ships with an Anthropic V4 Pro and it ships with an Anthropic V4 Pro and it ships with an Anthropic compatible endpoint, meaning Claude Code compatible endpoint, meaning Claude Code compatible endpoint, meaning Claude Code can run on it without you changing a can run on it without you changing a can run on it without you changing a thing about your workflow. Claude Code thing about your workflow. Claude Code thing about your workflow. Claude Code is the app, the part that opens your is the app, the part that opens your is the app, the part that opens your project, reads files, edits code, runs project, reads files, edits code, runs project, reads files, edits code, runs commands. The model behind it is what commands. The model behind it is what commands. The model behind it is what decides what to write and the model is decides what to write and the model is decides what to write and the model is what gets billed. By default, that's what gets billed. By default, that's what gets billed. By default, that's Anthropic at around $15 per million Anthropic at around $15 per million Anthropic at around $15 per million tokens. DeepSeek is around $1.74 tokens. DeepSeek is around $1.74 tokens. DeepSeek is around $1.74 for the same million. On the main test for the same million. On the main test for the same million. On the main test for real coding work, it's basically for real coding work, it's basically for real coding work, it's basically tied with the model Claude normally tied with the model Claude normally tied with the model Claude normally uses. Same quality output, same kind of uses. Same quality output, same kind of uses. Same quality output, same kind of results, just running on a model that results, just running on a model that results, just running on a model that costs a fraction of the price. The costs a fraction of the price. The costs a fraction of the price. The workload that was burning through the workload that was burning through the workload that was burning through the $200 plan drops to single digits. If you $200 plan drops to single digits. If you $200 plan drops to single digits. If you want to see exactly how to run DeepSeek want to see exactly how to run DeepSeek want to see exactly how to run DeepSeek V4 on Claude Code and take advantage of V4 on Claude Code and take advantage of V4 on Claude Code and take advantage of it, I've got a full walkthrough on how it, I've got a full walkthrough on how it, I've got a full walkthrough on how you can route different models on Claude you can route different models on Claude you can route different models on Claude Code. Comment DeepSeek and I'll send it Code. Comment DeepSeek and I'll send it Code. Comment DeepSeek and I'll send it to your DMs.
